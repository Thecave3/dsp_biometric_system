function [gmm]=feature_extraction(sample,data,sample_time);
% function  feature_extraction()
% extracts the gaussian mixture model from the sample audio
% sample_time represents the recording time for each representation



gmm = "feature";
end

